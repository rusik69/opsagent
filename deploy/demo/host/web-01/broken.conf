# Broken directive (typo: "listenn" instead of "listen") that makes
# `nginx -t` fail. The repos expect nginx healthy on port 8080.
server {
    listenn 8080;
    server_name web-01.example.com;
    root /var/www/html;
}